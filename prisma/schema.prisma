// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Event{
  id  Int @unique  @default(autoincrement())
  title String
  description String
  price Int
  date DateTime @default(now())
  createId Int
  CreatUser User @relation(fields: [createId],references: [id])


}

model User{
  id  Int @unique  @default(autoincrement())
  name String
  email String @unique
  password String
  createdEvents Event[]
}
